// Code generated by ent, DO NOT EDIT.

package ent

import (
	"asutp-server/ent/taskstatus"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// TaskStatus is the model entity for the TaskStatus schema.
type TaskStatus struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Code holds the value of the "code" field.
	Code string `json:"code,omitempty"`
	// IsTerminal holds the value of the "is_terminal" field.
	IsTerminal bool `json:"is_terminal,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TaskStatusQuery when eager-loading is set.
	Edges        TaskStatusEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TaskStatusEdges holds the relations/edges for other nodes in the graph.
type TaskStatusEdges struct {
	// Tasks holds the value of the tasks edge.
	Tasks []*Task `json:"tasks,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TasksOrErr returns the Tasks value or an error if the edge
// was not loaded in eager-loading.
func (e TaskStatusEdges) TasksOrErr() ([]*Task, error) {
	if e.loadedTypes[0] {
		return e.Tasks, nil
	}
	return nil, &NotLoadedError{edge: "tasks"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TaskStatus) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case taskstatus.FieldIsTerminal:
			values[i] = new(sql.NullBool)
		case taskstatus.FieldID:
			values[i] = new(sql.NullInt64)
		case taskstatus.FieldCode:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TaskStatus fields.
func (_m *TaskStatus) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case taskstatus.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case taskstatus.FieldCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field code", values[i])
			} else if value.Valid {
				_m.Code = value.String
			}
		case taskstatus.FieldIsTerminal:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_terminal", values[i])
			} else if value.Valid {
				_m.IsTerminal = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TaskStatus.
// This includes values selected through modifiers, order, etc.
func (_m *TaskStatus) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTasks queries the "tasks" edge of the TaskStatus entity.
func (_m *TaskStatus) QueryTasks() *TaskQuery {
	return NewTaskStatusClient(_m.config).QueryTasks(_m)
}

// Update returns a builder for updating this TaskStatus.
// Note that you need to call TaskStatus.Unwrap() before calling this method if this TaskStatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TaskStatus) Update() *TaskStatusUpdateOne {
	return NewTaskStatusClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TaskStatus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TaskStatus) Unwrap() *TaskStatus {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TaskStatus is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TaskStatus) String() string {
	var builder strings.Builder
	builder.WriteString("TaskStatus(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("code=")
	builder.WriteString(_m.Code)
	builder.WriteString(", ")
	builder.WriteString("is_terminal=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsTerminal))
	builder.WriteByte(')')
	return builder.String()
}

// TaskStatusSlice is a parsable slice of TaskStatus.
type TaskStatusSlice []*TaskStatus
