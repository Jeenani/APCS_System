// Code generated by ent, DO NOT EDIT.

package ent

import (
	"asutp-server/ent/priority"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Priority is the model entity for the Priority schema.
type Priority struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ColorHex holds the value of the "color_hex" field.
	ColorHex string `json:"color_hex,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int16 `json:"sort_order,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PriorityQuery when eager-loading is set.
	Edges        PriorityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PriorityEdges holds the relations/edges for other nodes in the graph.
type PriorityEdges struct {
	// Tasks holds the value of the tasks edge.
	Tasks []*Task `json:"tasks,omitempty"`
	// Notifications holds the value of the notifications edge.
	Notifications []*Notification `json:"notifications,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TasksOrErr returns the Tasks value or an error if the edge
// was not loaded in eager-loading.
func (e PriorityEdges) TasksOrErr() ([]*Task, error) {
	if e.loadedTypes[0] {
		return e.Tasks, nil
	}
	return nil, &NotLoadedError{edge: "tasks"}
}

// NotificationsOrErr returns the Notifications value or an error if the edge
// was not loaded in eager-loading.
func (e PriorityEdges) NotificationsOrErr() ([]*Notification, error) {
	if e.loadedTypes[1] {
		return e.Notifications, nil
	}
	return nil, &NotLoadedError{edge: "notifications"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Priority) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case priority.FieldID, priority.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case priority.FieldName, priority.FieldColorHex:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Priority fields.
func (_m *Priority) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case priority.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case priority.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case priority.FieldColorHex:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field color_hex", values[i])
			} else if value.Valid {
				_m.ColorHex = value.String
			}
		case priority.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				_m.SortOrder = int16(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Priority.
// This includes values selected through modifiers, order, etc.
func (_m *Priority) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTasks queries the "tasks" edge of the Priority entity.
func (_m *Priority) QueryTasks() *TaskQuery {
	return NewPriorityClient(_m.config).QueryTasks(_m)
}

// QueryNotifications queries the "notifications" edge of the Priority entity.
func (_m *Priority) QueryNotifications() *NotificationQuery {
	return NewPriorityClient(_m.config).QueryNotifications(_m)
}

// Update returns a builder for updating this Priority.
// Note that you need to call Priority.Unwrap() before calling this method if this Priority
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Priority) Update() *PriorityUpdateOne {
	return NewPriorityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Priority entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Priority) Unwrap() *Priority {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Priority is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Priority) String() string {
	var builder strings.Builder
	builder.WriteString("Priority(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("color_hex=")
	builder.WriteString(_m.ColorHex)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", _m.SortOrder))
	builder.WriteByte(')')
	return builder.String()
}

// Priorities is a parsable slice of Priority.
type Priorities []*Priority
